info:
  name: Get a product - negative case
  type: http
  seq: 6

http:
  method: GET
  url: https://dummyjson.com/products/{{randomId}}
  auth: inherit

runtime:
  variables:
    - name: ""
      value: ""
  assertions:
    - expression: res.body.title
      operator: eq
      value: "'Bruno Toy'"
      disabled: true

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
